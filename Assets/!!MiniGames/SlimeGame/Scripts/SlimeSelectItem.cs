using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Pet.Slime{
    public class SlimeSelectItem : MonoBehaviour{

        public int slimeIndex = 0;

        void OnClick() {
            //if (PopIt.PopMiniGame2.Instance && !PopIt.PopMiniGame2.Instance.isBusy) {
            //    if (GamesManager.Instace) {
            //        GamesManager.Instace.OpenSlimeGame();
            //    }

            //    if (SlimeGame.Instance) {
            //        SlimeGame.Instance.OpenSlime(slimeIndex);
            //    }

            //    SoundPlayer.PlaySound(SoundPlayer.SOUND_TAP);
            //    VibrationManager.Vibrate();

            //    AnalyticsEventer.AddLog("Play_All");
            //}
        }
    }
}